apiVersion: v1
kind: Service
metadata:
  name: micro-service-service
  namespace: micro-service
spec:
  selector:
    app: micro-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080